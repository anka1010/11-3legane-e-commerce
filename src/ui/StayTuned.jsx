import styles from "./StayTuned.module.scss";

function StayTuned() {
  return (
    <div className={`container-sm ${styles.text}`}>
      The page seems to be in development. Stay tuned!
    </div>
  );
}

export default StayTuned;
